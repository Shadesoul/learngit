#encoding=utf-8
mkdir learngit
cd learngit 
pwd #显示当前目录，不用于ls

git init #在当前目录建新仓库
git commit -a -m 'xxx' #跳过添加环节，直接提交

git diff xxx.txt #查看修改了什么内容，仅限于add前。
git reset --hard HEAD^ #回退到上一个版本
git reset --hard xxxxx #输入版本号可以回到未来版本。不如用分支更好。










